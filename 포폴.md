# PlanP 포트폴리오 정리

이 문서는 PlanP 프로젝트를 이력서, 포트폴리오, 면접 답변에 활용하기 위한 정리본입니다.

## 프로젝트 한 줄 소개

PlanP는 여행자가 도시별 교통, 맛집, 관광, 숙소 정보를 한 화면에서 탐색하고, 관심 항목 저장과 예약 링크 이동까지 이어갈 수 있도록 만든 여행 정보 통합 서비스입니다.

## 이력서용 요약

### 짧은 버전

여행 정보 통합 서비스 PlanP를 React, TypeScript, Node.js, Express, SQLite 기반으로 구현했습니다. 도시별 설문 결과 저장/복원, 관심 항목 보관함, 최근 본 항목, 예약 클릭 로그, 관리자 통계 페이지를 개발하고, JWT 인증과 DB migration, API validation, 대용량 데이터 code splitting, 공통 상태 UI를 적용했습니다.

### bullet 버전

- React, TypeScript, Vite 기반 여행 정보 통합 서비스 PlanP 개발
- 도시별 설문 결과를 로그인 사용자 기준으로 저장/복원하는 기능 구현
- 관심 항목, 최근 본 항목, 외부 예약 링크 클릭 로그를 SQLite DB에 저장
- JWT 인증과 관리자 권한 분리를 적용해 일반 사용자와 관리자 접근 제어 구현
- Express API를 기능별 route로 분리하고 zod 기반 request validation 적용
- DB schema migration 구조를 도입해 테이블 생성 및 변경 이력 관리
- 관리자 페이지에서 도시 선택 순위, 예약 클릭 수, 카테고리별 클릭 통계 제공
- 관리자 통계 CSV 다운로드 제공
- 대용량 장소 상세 데이터를 도시/타입별 동적 import로 분리해 초기 번들 부담 완화
- 서버 API 테스트와 수동 QA 체크리스트를 작성해 핵심 사용자 흐름 검증

## 포트폴리오용 상세 설명

### 프로젝트 목적

여행 준비 과정에서 교통, 맛집, 관광지, 숙소, 예약 플랫폼 정보가 여러 사이트에 흩어져 있는 문제를 해결하기 위해 만들었습니다. 사용자는 도시를 선택하고 간단한 설문을 진행한 뒤, 본인의 여행 성향에 맞는 이동수단, 맛집, 관광, 숙소 정보를 한 화면에서 확인할 수 있습니다.

초기 기획은 프론트엔드 중심의 여행 정보 탐색 서비스였지만, 포트폴리오 완성도를 높이기 위해 실제 서비스에서 필요한 로그인, 사용자별 데이터 저장, 예약 클릭 로그, 관리자 통계까지 백엔드와 DB로 확장했습니다.

### 주요 사용자 흐름

```txt
도시 선택
→ 설문 진행
→ 결과 페이지에서 교통/맛집/관광/숙소 확인
→ 관심 항목 저장
→ 상세 페이지 확인
→ 예약 링크 클릭
→ 클릭 로그 저장
```

로그인 사용자는 도시별 설문 결과가 DB에 저장됩니다. 같은 도시를 다시 선택하면 이전 기록 복구 또는 새로 작성하기를 선택할 수 있습니다.

```txt
로그인
→ 도쿄 설문 완료
→ survey_results 저장
→ 다시 도쿄 선택
→ 이전 기록 복구 또는 새로 작성하기 선택
```

## 담당 및 구현 범위

### Frontend

- React Router 기반 페이지 라우팅 구성
- 도시 선택, 설문, 결과, 상세, 보관함, 관리자 페이지 구현
- Redux Toolkit 기반 로그인 상태와 관심 항목 상태 관리
- Axios API client 구성
- 로그인 여부에 따른 저장/복원 흐름 처리
- 결과 페이지 카테고리 nav 및 쇼핑 준비중 화면 구현
- 상세 페이지 진입 시 최근 본 항목 저장
- 상세 페이지 위치 섹션 Street View 우선 노출
- 외부 예약 링크 클릭 시 booking log API 호출
- 관리자 접근 제한 라우트 구현
- 대용량 상세 데이터 lazy loading 및 code splitting 적용
- 공통 로딩/에러/빈 상태 UI 적용

### Backend

- Node.js, Express 기반 REST API 구현
- JWT 로그인/회원가입/내 정보 조회 API 구현
- bcrypt 기반 비밀번호 해시 저장
- 인증 middleware와 관리자 권한 middleware 구현
- favorites, survey_results, booking_clicks, recent_views API 구현
- 관리자 통계 API 구현
- zod 기반 request body validation 적용
- API route를 기능별 파일로 분리
- 서버 API 테스트 작성

### Database

- SQLite와 better-sqlite3 기반 로컬 DB 구성
- users, favorites, survey_results, booking_clicks, recent_views 테이블 설계
- schema_migrations 테이블을 통한 migration 이력 관리
- user_id + city_code 기준 설문 결과 upsert 구조 구현
- user_id + item_type + item_id 기준 관심 항목 중복 저장 방지
- 관리자 계정 seed script 작성

## 기술 스택

### Frontend

- React
- TypeScript
- Vite
- React Router
- Redux Toolkit
- Axios
- Tailwind CSS
- lucide-react

### Frontend Architecture Highlights

1. Redux Toolkit + React Router v6 조합으로 전역 상태와 SPA 라우팅 구조를 분리해 관리했습니다.
2. react-virtualized를 사용해 대용량 리스트를 가상화하고, 무한 스크롤에 맞는 렌더링 부담을 줄였습니다.
3. React.lazy와 Suspense를 적용해 라우트 단위 코드 스플리팅을 구성하고, 초기 진입 속도를 개선했습니다.
4. Tailwind CSS 기반으로 반응형 레이아웃을 구성하고, shadcn/ui 패턴의 공통 컴포넌트를 일부 도입해 UI 재사용성을 높였습니다.
5. JWT 인증과 role 기반 권한 제어로 로그인 세션과 관리자 접근을 분리했습니다.
6. React.memo, useMemo, useCallback을 필요한 지점에 적용해 불필요한 리렌더링을 줄였습니다.
7. Axios를 중심으로 REST API를 연동하고, 일부 보조 로직은 Fetch API로 처리했습니다.

### Backend

- Node.js
- Express
- SQLite
- better-sqlite3
- JWT
- bcrypt
- zod

### Verification

- TypeScript build
- Vite production build
- Node built-in test runner
- 임시 SQLite DB 기반 API 테스트
- 수동 QA 체크리스트

## 주요 기능 정리

### 1. 도시별 설문 결과 저장 및 복원

로그인 사용자가 설문을 완료하면 도시별 결과를 `survey_results` 테이블에 저장합니다. 같은 사용자가 같은 도시의 설문을 다시 저장하면 새 데이터를 추가하는 대신 기존 row를 갱신합니다.

포트폴리오 설명:

```txt
사용자별 도시 설문 결과를 DB에 저장하고, 같은 도시 재진입 시 이전 기록 복구 또는 새 설문 작성을 선택할 수 있도록 구현했습니다.
```

### 2. 관심 항목 보관함

사용자가 마음에 드는 교통, 맛집, 관광, 숙소 항목을 저장할 수 있습니다. 저장된 항목은 로그인 사용자 기준으로 DB에 유지되고, 보관함 페이지에서 다시 확인할 수 있습니다.

포트폴리오 설명:

```txt
관심 항목을 사용자 계정 기준으로 저장하고, 새로고침 이후에도 유지되도록 favorites API와 DB 테이블을 연결했습니다.
```

### 3. 최근 본 항목

로그인 사용자가 맛집, 관광, 숙소 상세 페이지에 진입하면 최근 본 항목으로 저장됩니다. 같은 항목을 다시 보면 조회 시간이 갱신됩니다.

포트폴리오 설명:

```txt
상세 페이지 진입 이벤트를 사용자 행동 데이터로 저장해 보관함에서 최근 본 항목을 확인할 수 있도록 구현했습니다.
```

### 4. 예약 클릭 로그

사용자가 외부 예약 링크를 클릭하면 도시, 카테고리, 상품명, 플랫폼, 링크가 `booking_clicks` 테이블에 저장됩니다. 로그인하지 않은 사용자의 클릭도 집계할 수 있도록 `user_id`는 nullable로 설계했습니다.

포트폴리오 설명:

```txt
외부 예약 링크 클릭을 행동 로그로 저장하고, 관리자 페이지에서 도시별/카테고리별 클릭 통계로 확인할 수 있도록 구현했습니다.
```

### 5. 관리자 페이지

관리자는 `/admin` 페이지에서 서비스 사용 데이터를 확인할 수 있습니다.

관리자 통계:

- 가장 많이 선택한 도시
- 도시 선택 순위
- 도시별 예약 클릭 수
- 이동수단, 맛집, 관광, 숙소별 클릭 수
- 많이 클릭한 예약 상품
- 도시, 카테고리, 기간 필터
- CSV 다운로드

포트폴리오 설명:

```txt
사용자 행동 데이터를 기반으로 관리자 통계 페이지를 구현해 서비스 운영 관점의 데이터를 확인할 수 있도록 했습니다.
```

### 6. 인증 및 권한 제어

JWT 기반 로그인 구조를 적용했습니다. 일반 사용자와 관리자는 `users.role` 값으로 구분하며, 관리자 페이지는 프론트 라우트와 백엔드 API 양쪽에서 접근을 제한합니다.

포트폴리오 설명:

```txt
JWT 인증과 role 기반 권한 제어를 적용해 일반 사용자와 관리자 기능을 분리했습니다.
```

### 7. API 검증 및 백엔드 구조 분리

Express API를 기능별 route 파일로 분리하고, zod schema로 요청 body를 검증합니다. 잘못된 데이터는 DB에 접근하기 전에 `400` 응답으로 차단합니다.

포트폴리오 설명:

```txt
API 입력값을 zod schema로 검증하고, 기능별 route 분리를 적용해 백엔드 유지보수성을 개선했습니다.
```

### 8. DB migration 관리

테이블 생성 SQL을 `server/migrations`로 분리하고, 서버 시작 시 적용되지 않은 migration만 실행하도록 구성했습니다.

포트폴리오 설명:

```txt
DB schema migration 구조를 도입해 테이블 생성과 변경 이력을 코드로 관리했습니다.
```

### 9. 프론트 성능 최적화

상세 페이지는 route 단위 lazy loading으로 분리했고, 대용량 장소 상세 데이터는 도시/타입별 동적 import로 분리했습니다. 기존에는 약 1MB 크기의 단일 상세 데이터 청크가 생성됐지만, 최적화 후 여러 개의 작은 청크로 나뉘도록 개선했습니다.

포트폴리오 설명:

```txt
대용량 장소 상세 데이터를 도시/타입 단위로 분리하고, 상세 페이지 진입 시점에만 동적으로 로딩해 초기 번들 부담을 줄였습니다.
```

### 10. 상세 위치 데이터 정밀화

상세 페이지 위치 섹션은 Google Street View Static API를 우선 사용하고, 좌표가 있는 항목은 좌표 기반으로 더 정확하게 맞추도록 개선했습니다. 스트릿뷰가 없거나 실패하면 기존 이미지를 폴백으로 사용해 화면이 비지 않도록 처리했습니다.

포트폴리오 설명:

```txt
상세 페이지 위치 섹션에 Street View 우선 노출과 좌표 기반 정밀화를 적용하고, 실패 시 기존 이미지로 폴백하도록 구성했습니다.
```

### 11. 공통 상태 UI

로딩, 에러, 빈 상태를 `PageState` 공통 컴포넌트로 분리해 즐겨찾기, 서베이 결과, 관리자 페이지에 동일한 상태 표현을 적용했습니다.

포트폴리오 설명:

```txt
로딩/에러/빈 상태를 공통 컴포넌트로 통합해 페이지별 상태 표현을 일관되게 정리했습니다.
```

### 12. UI 재사용성과 화면 일관성

Tailwind CSS를 기반으로 화면을 구성하고, 버튼/입력/탭/테이블/배지 같은 공통 컴포넌트를 재사용하도록 정리했습니다. 최근 본 항목과 관리자 통계처럼 비슷한 정보 블록은 같은 카드 패턴을 공유하도록 맞췄습니다.

포트폴리오 설명:

```txt
Tailwind 기반 UI에 공통 컴포넌트를 정리해 화면 간 일관성을 맞추고, 관리자 통계와 사용자 보관함에서 같은 카드 패턴을 재사용하도록 구성했습니다.
```

## DB 테이블 설계 설명

### users

회원 계정 정보입니다. 비밀번호는 원문이 아니라 bcrypt hash로 저장합니다.

핵심 컬럼:

- `login_id`
- `password_hash`
- `name`
- `email`
- `role`
- `created_at`

### survey_results

사용자의 도시별 설문 결과입니다.

핵심 설계:

- `user_id`
- `city_code`
- `answers`
- `result_type`
- `created_at`
- `updated_at`

`user_id + city_code` 조합을 unique하게 관리해 같은 도시 결과는 갱신되도록 설계했습니다.

### favorites

사용자가 저장한 관심 항목입니다.

핵심 설계:

- `user_id`
- `item_id`
- `item_type`
- `title`
- `image`
- `href`
- `payload`
- `created_at`

카드마다 데이터 구조가 조금씩 다를 수 있어 원본 데이터를 `payload`에도 함께 저장했습니다.

### booking_clicks

외부 예약 링크 클릭 로그입니다.

핵심 설계:

- `user_id`
- `city_code`
- `item_type`
- `item_title`
- `platform`
- `href`
- `section_label`
- `created_at`

비로그인 사용자의 클릭도 저장할 수 있도록 `user_id`는 nullable로 설계했습니다.

### recent_views

로그인 사용자의 최근 본 상세 항목입니다.

핵심 설계:

- `user_id`
- `item_id`
- `item_type`
- `title`
- `href`
- `payload`
- `viewed_at`

같은 상세 항목을 다시 보면 `viewed_at`을 갱신하는 방식으로 최근 본 순서를 관리합니다.

## API 정리

### Auth

```txt
POST /auth/signup
POST /auth/login
GET /auth/me
```

회원가입, 로그인, 현재 로그인 사용자 조회 API입니다.

### Favorites

```txt
GET /favorites
POST /favorites
DELETE /favorites/:itemType/:itemId
```

관심 항목 조회, 저장, 삭제 API입니다.

### Survey Results

```txt
GET /survey-results/:cityCode
POST /survey-results
```

도시별 설문 결과 조회와 저장 API입니다.

### Booking Clicks

```txt
POST /booking-clicks
```

외부 예약 링크 클릭 로그 저장 API입니다.

### Recent Views

```txt
GET /recent-views
POST /recent-views
```

최근 본 항목 조회와 저장 API입니다.

### Admin

```txt
GET /admin/stats
GET /admin/stats?cityCode=tokyo&itemType=transport&range=30d
```

관리자 통계 조회 API입니다. 관리자 권한이 필요합니다.

## 검증 내용

### 자동 검증

```bash
npm run build
npm --prefix server test
```

확인한 내용:

- TypeScript build 성공
- Vite production build 성공
- 서버 API 테스트 통과
- 인증, 설문 저장, booking log, 관리자 권한, 최근 본 항목 API 테스트

### 수동 QA

`docs/qa-checklist.md`에 다음 흐름을 체크리스트로 정리했습니다.

- 회원가입/로그인/로그아웃
- 비로그인 설문
- 로그인 설문 저장
- 이전 기록 복구
- 관심 항목 저장/삭제
- 최근 본 항목
- 예약 클릭 로그
- 관리자 접근 제한
- 관리자 통계 필터
- 상세 데이터 code splitting 확인

## 성과 및 개선 포인트

### 기능적 성과

- 단순 정적 여행 정보 페이지에서 사용자별 데이터 저장이 가능한 서비스 구조로 확장
- 로그인 사용자 기준 설문 결과 저장/복원 기능 구현
- 예약 링크 클릭 로그 기반 관리자 통계 기능 구현
- 관리자 권한 분리로 운영자 화면 접근 제어 구현

### 기술적 성과

- 프론트엔드, 백엔드, DB를 연결한 end-to-end 흐름 구현
- REST API와 JWT 인증 구조 구현
- DB migration 구조 도입
- zod 기반 API validation 적용
- 대용량 프론트 데이터 code splitting 적용
- 서버 API 테스트와 수동 QA 문서 작성
- 관리자 통계 CSV 다운로드 구현
- 공통 로딩/에러/빈 상태 UI 정리
- 상세 페이지 Street View 우선 노출 및 좌표 기반 정밀화 적용

### 개선한 문제

기존 문제:

```txt
결과 페이지와 상세 페이지에서 큰 장소 상세 데이터가 한 번에 번들에 포함됨
```

개선:

```txt
상세 페이지 lazy loading + 도시/타입별 상세 데이터 동적 import 적용
```

결과:

```txt
약 1MB 단일 cityPlaceDetails 청크를 제거하고 여러 개의 작은 상세 데이터 청크로 분리
```

## 면접 답변 준비

### Q. 이 프로젝트에서 가장 신경 쓴 부분은 무엇인가요?

사용자 흐름이 실제 서비스처럼 이어지는지를 가장 신경 썼습니다. 단순히 정보를 보여주는 데서 끝내지 않고, 로그인 사용자의 설문 결과 저장, 관심 항목 보관, 최근 본 항목, 예약 클릭 로그, 관리자 통계까지 연결했습니다. 이를 통해 사용자의 행동이 DB에 저장되고, 관리자 화면에서 다시 활용되는 구조를 구현했습니다.

### Q. 프론트엔드 지원자인데 백엔드와 DB를 왜 붙였나요?

실무에서는 프론트엔드도 API 구조와 데이터 저장 흐름을 이해해야 한다고 생각했습니다. 그래서 로그인, JWT 인증, 사용자별 데이터 저장, 관리자 통계 API를 직접 구현해 프론트가 백엔드와 어떤 방식으로 데이터를 주고받는지 경험했습니다.

### Q. DB 설계에서 고려한 점은 무엇인가요?

사용자별로 저장되어야 하는 데이터와 전체 통계로 집계되어야 하는 데이터를 구분했습니다. 설문 결과는 `user_id + city_code` 기준으로 중복 저장을 막고 갱신되도록 했고, 예약 클릭 로그는 비로그인 사용자도 남길 수 있게 `user_id`를 nullable로 설계했습니다.

### Q. 관리자 페이지는 왜 만들었나요?

예약 링크 클릭 로그를 저장하는 것에서 끝내지 않고, 그 데이터를 서비스 운영 관점에서 확인할 수 있도록 하기 위해 만들었습니다. 도시별 예약 클릭 수, 카테고리별 클릭 수, 많이 클릭된 상품을 확인할 수 있어 사용자 행동 분석 기능으로 확장할 수 있습니다.

### Q. API 안정성을 위해 어떤 처리를 했나요?

인증이 필요한 API에는 JWT middleware를 적용했고, 관리자 API에는 관리자 권한 middleware를 추가했습니다. 또한 zod schema로 request body를 검증해 잘못된 데이터가 DB에 저장되지 않도록 했습니다.

### Q. 성능 최적화는 어떤 걸 했나요?

상세 페이지를 lazy loading으로 분리했고, 대용량 장소 상세 데이터도 도시/타입 단위로 분리했습니다. 사용자가 상세 페이지에 진입했을 때 필요한 데이터만 동적으로 import하도록 바꿔 초기 번들 부담을 줄였고, 이미지 로딩과 공통 상태 UI도 함께 정리했습니다.

### Q. 테스트는 어떻게 했나요?

서버 API는 Node built-in test runner와 임시 SQLite DB를 사용해 테스트했습니다. 회원가입/로그인, 설문 저장, 예약 클릭 로그, 관리자 권한, 최근 본 항목 기능을 검증했습니다. 프론트와 전체 사용자 흐름은 `docs/qa-checklist.md`에 수동 QA 기준을 작성해 확인할 수 있도록 했습니다.

### Q. 아쉬운 점이나 다음 개선점은 무엇인가요?

현재는 로컬 SQLite 기반이기 때문에 실제 배포 환경에서는 PostgreSQL 같은 운영 DB로 전환하는 것이 좋습니다. 또한 관리자 통계 시각화, 프론트 E2E 테스트, 에러 상태 UI를 더 보강하면 실서비스에 가까운 완성도를 만들 수 있습니다.

### Q. 프론트엔드에서 전역 상태와 라우팅은 어떻게 구성했나요?

Redux Toolkit으로 사용자 인증 상태, 즐겨찾기, 최근 본 항목 같은 전역 데이터를 관리했고, React Router v6로 페이지 구조를 분리했습니다. 인증이 필요한 라우트는 보호 라우트로 감싸고, 페이지 단위로 lazy loading을 적용해 SPA 구조와 성능을 같이 챙겼습니다.

### Q. UI는 어떤 기준으로 정리했나요?

Tailwind CSS를 기본 스타일링 도구로 사용했고, 버튼/탭/입력/테이블/배지 같은 공통 요소는 재사용 가능한 컴포넌트로 정리했습니다. 최근 본 항목과 관리자 통계처럼 비슷한 정보 블록은 동일한 카드 패턴을 사용해 화면 간 일관성을 맞췄습니다.

## 이력서에 넣기 좋은 문장

### 1문장 버전

React, TypeScript, Node.js, Express, SQLite 기반 여행 정보 통합 서비스를 개발하고, 사용자별 설문 결과 저장/복원, 관심 항목, 예약 클릭 로그, 관리자 통계, JWT 인증, API validation, code splitting을 구현했습니다.

### 3문장 버전

React와 TypeScript 기반으로 도시별 여행 정보를 탐색하고 예약 플랫폼으로 이동할 수 있는 여행 정보 통합 서비스를 개발했습니다. Node.js, Express, SQLite를 연결해 로그인 사용자별 설문 결과 저장/복원, 관심 항목, 최근 본 항목, 예약 클릭 로그를 구현했습니다. 관리자 통계 페이지, JWT 인증, zod 기반 API validation, DB migration, 대용량 데이터 code splitting, 공통 상태 UI, Street View 정밀화, CSV 다운로드까지 적용해 실무형 프로젝트 구조로 확장했습니다.

### 경력기술서 스타일

```txt
PlanP | 여행 정보 통합 서비스
- React, TypeScript, Vite 기반 프론트엔드 구현
- Node.js, Express, SQLite 기반 백엔드 및 DB 연동
- JWT 인증, bcrypt 비밀번호 해시, role 기반 관리자 권한 구현
- 사용자별 설문 결과 저장/복원, 관심 항목, 최근 본 항목 기능 구현
- 외부 예약 링크 클릭 로그 저장 및 관리자 통계 페이지 구현
- zod request validation, 기능별 API route 분리, DB migration 구조 도입
- 대용량 장소 상세 데이터 동적 import로 초기 번들 부담 완화
- 서버 API 테스트 및 수동 QA 체크리스트 작성
- 관리자 통계 CSV 다운로드, Street View 우선 노출, 공통 상태 UI 적용
```

## 포트폴리오 페이지에 넣을 구성

### 제목

```txt
PlanP - 여행 정보 통합 및 예약 동선 지원 서비스
```

### 부제

```txt
흩어진 여행 정보를 한 화면에 모으고, 사용자 행동 데이터를 저장해 개인화와 관리자 통계까지 확장한 풀스택형 프론트엔드 프로젝트
```

### 핵심 기능

- 도시별 설문 기반 여행 정보 탐색
- 로그인 사용자별 설문 결과 저장 및 복원
- 관심 항목 보관함
- 최근 본 상세 항목
- 예약 링크 클릭 로그
- 관리자 통계 페이지
- 쇼핑 카테고리 준비중 화면

### 기술적 강조점

- JWT 인증 및 관리자 권한 제어
- SQLite 기반 사용자 데이터 저장
- Express API route 분리
- zod 기반 request validation
- DB migration 관리
- 대용량 데이터 code splitting
- React Router v6 기반 SPA 라우팅
- Redux Toolkit 기반 전역 상태 관리
- react-virtualized 기반 대용량 리스트 가상화
- Tailwind CSS + shadcn/ui 패턴의 재사용 컴포넌트
- React.memo, useMemo, useCallback 기반 렌더링 최적화
- API 테스트 및 QA 문서화

### 문제 해결 사례

```txt
문제:
장소 상세 데이터가 커지면서 빌드 결과에 약 1MB 크기의 단일 상세 데이터 청크가 생성됨

해결:
상세 페이지 lazy loading과 도시/타입별 동적 import 구조를 적용

결과:
초기 로딩에 포함되는 대용량 데이터를 줄이고, 사용자가 상세 페이지에 진입할 때 필요한 데이터만 로딩하도록 개선
```

## GitHub README에 강조하면 좋은 키워드

- React
- TypeScript
- Vite
- Redux Toolkit
- React Router
- Node.js
- Express
- SQLite
- JWT Auth
- Role-based Authorization
- API Validation
- DB Migration
- Code Splitting
- Admin Dashboard
- User Behavior Logging
- QA Checklist

## 현재 한계와 개선 계획

### 현재 한계

- 로컬 SQLite 기반이라 운영 배포 시 DB 영속성 고려 필요
- 쇼핑 카테고리는 준비중 화면만 제공
- 프론트 E2E 테스트는 아직 미구현
- 관리자 통계 시각화는 기본 형태

### 개선 계획

- PostgreSQL 전환
- 배포 환경 구성
- Playwright 기반 E2E 테스트 추가
- 관리자 통계 차트 고도화
- 에러/로딩/빈 상태 UI 추가 보강
- 실제 예약 API 또는 공식 데이터 연동 검토

## 최종 어필 포인트

이 프로젝트는 단순한 화면 구현을 넘어, 사용자의 행동이 DB에 저장되고 관리자 통계로 이어지는 서비스 흐름을 구현한 것이 핵심입니다. 프론트엔드 중심 프로젝트이지만 백엔드 API, 인증, DB, 테스트, 성능 최적화, QA 문서까지 연결해 실무에서 필요한 전체 개발 흐름을 경험한 프로젝트로 설명할 수 있습니다.
